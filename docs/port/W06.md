# W06 – Friedliche Tiere und Battle Mobs

## R21-Nachzug

Portierer `w06-crosscut-r21`, Stand 2026-09-11. Wendet `DECISIONS.md` R21 auf den Dimensionscode aus W05 an. Keine
eigene Originalklasse. **Nicht mit Gradle gebaut und nicht getestet** (Parallelwelle, der Integrator baut). Gelaufen
ist nur ein `javac` über `world/gen`, `world/dimension` und `R21GameTests` gegen `build/classes/java/main` des
W05-Baus, Ausgabe im Scratchpad, fehlerfrei – das ersetzt weder `./gradlew build` noch `runGameTests`.

Quellen: 1.7.10-Vanilla aus `reference/jar/mcp/client-1.7.10.jar`, mit Vineflower 1.10.1 dekompiliert (`aia`
BiomeDecorator, `aid` BiomeGenHills, `ahu` BiomeGenBase, `arn`, `arc`, `asq`, `asn`, `ard`), Namen über `joined.srg`
und `fields.csv`/`methods.csv`, Einzelstellen mit `javap -c` gegengeprüft (`aji.<init>` @47-52 für `isFullBlock`).

### 1. Ein Weltseed-Pfad

| Datei | Änderung |
|---|---|
| `world/dimension/crystal/ChunkProviderOreSpawn5.java` | `createState` überschrieben, baut `Noises` aus dem Seed; `noises()` fällt wie Utopia/Mining/Danger auf den Server-Seed zurück. `fillFromNoise`, `getBaseHeight`, `getBaseColumn` fragen den `RandomState` nicht mehr |
| `world/dimension/chaos/ChunkProviderOreSpawn6.java` | dasselbe |
| `world/gen/LegacyChunk.java` | `legacyWorldSeed(RandomState)` und der Schlüssel `orespawn:legacy_world_seed` entfallen |

Wirkung: Crystal- und Chaos-Gelände hängen am selben Wert wie die übrigen vier (1.7.10 `worldObj.getSeed()`). In einer
bestehenden Welt passen alte Crystal-/Chaos-Chunks an der Grenze nicht mehr zu neu generierten.

### 2. Mining dekoriert mit dem 1.7.10-Java-Decorator

Gegen `ChunkProviderOreSpawn2.populate` und die dekompilierten Vanilla-Klassen geprüft:

| Schritt | Original | Port |
|---|---|---|
| Strukturen | :283-287, zuerst | `super.applyBiomeDecoration` zuerst (vorher zwischen Dungeons und Emerald) |
| Wassersee | :288, `nextInt(4)`, Y `nextInt(256)` | gleich, `WorldGenLakes` |
| Lavasee | :294-300, `nextInt(8)`, Y `nextInt(nextInt(248)+8)`, unter Y 63 oder `nextInt(10) == 0` | gleich |
| Dungeons | :306-311, 8 Versuche | geteiltes `WorldGenDungeons` statt Vanilla-`MonsterRoomFeature` |
| `biomegenbase.decorate` | :312, `extremeHills` = `new aid(3, false)` | `BiomeGenHills.decorate`: `BiomeDecorator.extremeHills()` (Bäume 0, Blumen 2, Gras 1, Sand 3, Ton 1, Kies 1, Quellen 50/20), dann Emerald 3–8 (Y 4–31) und 7 Silberfisch-Adern (Y 0–63) |
| Anfangs-Spawns | :313 | `spawnOriginalMobs`, unverändert |
| Eis/Schnee | :314-326 | entfällt, Temperatur 0,8 zieht keine Zahl und setzt nichts |
| OreSpawnWorld-Erze | FML nach `populate` | `OreSpawnWorldOres.generateMining`, jetzt über `LegacyWorld`/`Random` |

Bäume: `aia` zieht in jedem Chunk `nextInt(10)`, Extreme Hills trifft damit in etwa jedem zehnten Chunk einen Baum.
`aid.a(Random)` nimmt in 2 von 3 Fällen `WorldGenTaiga2`, sonst `ahu.a(Random)`: 1 von 10 `WorldGenBigTree`, sonst
`WorldGenTrees`. Alle drei sind neu portiert.

| Datei | Änderung |
|---|---|
| `world/dimension/mining/ChunkProviderOreSpawn2.java` | `applyBiomeDecoration` = Strukturen, dann `populate` (neu); `writeBlocks` → `LegacyChunk.fillChunk` |
| `data/orespawn/worldgen/biome/mining.json` | `features: []`. Vorher Vanilla-Referenzen (`disk_*`, `spring_*`, `trees_windswept_hills`, `flower_default`, `patch_grass_badlands`, Pilze, Zuckerrohr, Kürbis, `freeze_top_layer`); eigene Feature-JSON gab es nicht |
| `world/gen/BiomeGenHills.java` | `decorate(LegacyWorld, Random, x, z)` ruft zuerst den Decorator (das `super.decorate` des Originals); neu `getRandomWorldGenForTrees` |
| `world/gen/BiomeGenBase.java` | neu `getRandomWorldGenForTrees`, `pickFlower`, `getRandomWorldGenForGrass` (vorher im Decorator fest verdrahtet) |
| `world/gen/WorldGenAbstractTree.java`, `WorldGenTrees.java`, `WorldGenTaiga2.java`, `WorldGenBigTree.java` | neu: `arc`, `asq`, `asn`, `ard` |
| `world/gen/WorldGenerator.java` | `doBlockNotify`, `setScale`, `setBlockAndNotifyAdequately` ergänzt |
| `world/gen/OreSpawnWorldOres.java` | `LegacyWorld` + `java.util.Random` statt `LevelAccessor` + `RandomSource` |

Wirkung auf das Mining-Gelände: ab dem ersten Populate-Schritt eine andere Zufallsfolge (Vanilla-`MonsterRoomFeature`
zog anders), Vegetation und Quellen in 1.7.10-Dichte, dazu Fichten, Eichen und große Eichen. Kies- und Sandadern über
Luft bekommen ihren Fall-Tick, wie über `onBlockAdded` in 1.7.10. Seen prüfen Himmelslicht jetzt über die Heightmap
statt über die Licht-Engine.

### 3. Eine Kopie der Hilfsklassen

| vorher | jetzt |
|---|---|
| `world/dimension/utopia/legacy/` (13 Klassen) | `world/gen/`: `BiomeDecorator`, `LegacyWorld`, `WorldGenerator`, `WorldGenMinable`, `WorldGenLakes`, `WorldGenClay`, `WorldGenDungeons`, `WorldGenFlowers`, `WorldGenLiquids`, `WorldGenPumpkin`, `WorldGenReed`, `WorldGenSand`, `WorldGenTallGrass`; Verzeichnis gelöscht |
| `world/gen/BiomeDecorator` (nur Erze, statisch, `RandomSource`) | mit der Utopia-Fassung zusammengeführt; statisch bleibt `generateOres(LegacyWorld, Random, x, z)` für Chaos |
| `world/gen/WorldGenMinable`, `WorldGenLakes` (`LevelAccessor`/`WorldGenLevel`) | durch die Utopia-Fassung ersetzt |
| `world/dimension/crystal/LegacyChunk` | `world/gen/LegacyChunk`, dazu `populateRandom` (vorher in `ChunkProviderOreSpawn`) und `fillChunk(ChunkAccess, BlockState[])` (vorher `writeBlocks` doppelt in Utopia und Mining); alte Datei gelöscht |
| Rauschen | lag schon nur in `world/gen/` (`NoiseGenerator*`); keine Kopie gefunden, nichts zusammenzuführen |

Die Utopia-Fassung gewinnt, weil sie 1.7.10 genauer trifft: Y-Grenzen 0–255, Fall-Tick für Fallblöcke, Licht aus der
Heightmap (die 1.21.1-Licht-Engine läuft erst nach FEATURES). Angepasste Aufrufer: `utopia/ChunkProviderOreSpawn`
(Imports, `fillChunk`, `populateRandom`), `village/ChunkProviderOreSpawn3`, `mining/ChunkProviderOreSpawn2`,
`crystal/ChunkProviderOreSpawn5`, `chaos/ChunkProviderOreSpawn6` (Erzschritt über `LegacyWorld` und
`LegacyChunk.populateRandom`).

Geprüft statt angenommen: `BitRandomSource.nextDouble` multipliziert mit `1.110223E-16F`, und dieses Float ist exakt
2^-53. `LegacyRandomSource` zieht damit dieselbe Folge wie `java.util.Random`; der Wechsel auf `Random` in Chaos und
Mining ändert keine Zahl.

Neu für alle Nutzer von `LegacyWorld`: `setBlock` plant für Laub einen Tick (Verzögerung 1). 1.21.1-Laub mit Distanz 7
zerfällt beim ersten Random-Tick, 1.7.10-Laub mit Metadaten 0 erst nach einer Nachbaränderung; der Tick setzt die
Distanz. Utopia und VillageMania setzen über den Decorator kein Laub.

### 4. Äxte in `sharp_weapon`

`data/minecraft/tags/item/enchantable/sharp_weapon.json`, `replace: false`: die acht `ItemAxe`-Unterklassen laut
`manifest.json` → `superclass_chain` (`ultimateaxe`, `emeraldaxe`, `rubyaxe`, `amethystaxe`, `crystalwoodaxe`,
`crystalpinkaxe`, `crystalstoneaxe`, `tigerseye_axe`). Battle Axe und Queen Battle Axe erben `UltimateSword` und
standen schon drin.

### 5. Fernschreiben: Reichweite und GameTest

Statische Reichweite für das Mining-Populate von Chunk C (Ursprung `C·16+8 … C·16+23`, Erze `C·16+0 … 15`, Aderzentrum
+8):

| Schritt | weitester Block um den Ursprung |
|---|---|
| Seebox | x−8 … x+7 |
| Blumen, Gras, Pilze, Kürbis | ±7 |
| große Eiche | Ast ±5 plus Laub ±2 |
| Fichte / Eiche | ±3 / ±2 |
| Sand-, Kies-, Tonscheibe | ±6 |
| Dungeon | ±4 |
| Erzader (32) | Zentrum +8, ±4, ±2,5 |

Alle Schreib- und Lesezugriffe bleiben in C und C+1 und damit im Schreibradius 1. **Das Populate-Fenster von Mining
ist nicht verschoben.** Utopia verschiebt weiter auf C−(1,1); auch das liegt im Radius.

`gametest/R21GameTests.java`:

| Test | Beleg |
|---|---|
| `miningAndUtopiaDecorateFiveByFiveFullChunksWithoutAFarChunkWrite` | je 5×5 `FULL`-Chunks in Mining und Utopia auf einer Fläche, deren 9×9-Umgebung weder geladen noch gespeichert ist (`getChunkNow`, `chunkMap.read`); log4j-Appender am Logger-Config von `net.minecraft.Util` in allen Log-Kontexten; keine `Detected setBlock in a far chunk`. Gegen „Stille ist kein Beleg“: Infested Stone in Mining, Blumen oder Gras in beiden Feldern; `orespawn:mining` listet 0 Placed Features |
| `crystalAndChaosTakeTheWorldSeedFromCreateState` | gleicher `createState`-Seed mit anderem `RandomState`: 0 Unterschiede; anderer `createState`-Seed: Unterschiede (je drei Chunks) |
| `orespawnAxesTakeSharpnessLikeTheir1710ItemAxe` | acht Äxte im Tag, `Sharpness.canEnchant` wahr |

Schlägt der erste Test mit `mining:` an, wird `ChunkProviderOreSpawn2.applyBiomeDecoration` wie Utopia auf
`populate(level, pos.x - 1, pos.z - 1)` umgestellt.

### PORT-Abweichungen

- `BiomeGenBase.getRandomWorldGenForTrees` baut je Aufruf einen neuen Baumgenerator. Im Original teilte sich das Biom
  ein Objekt, und `WorldGenBigTree.heightLimit` blieb nach der ersten großen Eiche für alle weiteren stehen – abhängig
  davon, welcher Chunk zuerst dekoriert wurde, und nicht threadsicher (R18 Fall 3).
- `WorldGenTrees`: nur `asq(boolean)`; der Lianen-/Kakao-Zweig zieht bei `vinesGrow = false` keine Zahl.
- `LegacyWorld.setBlock`: Laub-Tick (Abschnitt 3). `LegacyWorld.isFullBlock`: `isSolidRender` oder Laub.
- `WorldGenAbstractTree`: Erde schließt Grobe Erde und Podsol ein, Stämme auch `*_wood` (Metadaten desselben
  Originalblocks).
- `WorldGenBigTree`: `(int)` → `Mth.floor` am Astansatz (R20), für Y ≥ 0 gleich.

### Offen

- R21 verlangt den Java-Decorator in **allen** eigenen Dimensionen. Umgestellt ist laut Auftrag nur Mining. Crystal
  (`worldgen/biome/crystal.json`: Zuckerrohr, Pilze) und Chaos (`chaos.json`; Decorator laut world-03.md:395: Blumen 2,
  Gras 4, Bäume 1) laufen weiter über Vanilla-Features; Chaos portiert nur den Erzschritt.
- Nichts davon ist mit Gradle gebaut oder gelaufen; die Reichweite ist gerechnet, erst `runGameTests` belegt sie.
  Gerendert oder gespielt ist nichts (R17). *(Stand vor der Integration; der Bau und die Tests folgen unten.)*

## Integration (Stand nach dem Bau, 2026-09-11)

Zusammengeführt aus sechs Portierern: `w06-birds-bugs`, `w06-cows-fish`, `w06-herbivores`, `w06-cannonfodder`,
`w06-riders-ghosts`, `w06-crosscut-r21`.

Belegt: `./gradlew build` grün im **ersten** Lauf – kein einziger Kompilierfehler in den rund 70 neuen Klassen der
Portierer, nur die bekannten Deprecations (`EventBusSubscriber.bus()` aus W01, `makeMockServerPlayerInLevel` aus
W04). `./gradlew runGameTests`: **133 von 133** bestanden (104 bisherige plus 29 neue W06). Serverlog (`latest.log`
und Gradle-Konsole) ohne `/(ERROR|FATAL)\]`, ohne `Exception`, `Failed to create mod instance`, `Cowardly refusing`,
`Failed to parse`, `Can't keep up` und `Detected setBlock in a far chunk`. Statisch gegengeprüft (Python gegen
`manifest.json`): alle 29 Entity-Ids und 28 Eier existieren dort, jede Legacy-Id der Portierer stimmt mit
`ctor_args[0]` überein, jede Id hat einen Lang-Schlüssel, jedes Ei Item-Modell und Textur. Kein `TODO W06` mehr
unter `src/`.

**Gerendert oder gespielt ist nichts** (R17): Modelle, Kuh-Glanz, Geist-Transluzenz, Hut-Texturen, Tshirt-UV und
Wal-Fontäne sind nur gegen Quellen gebaut. `runServer` (R17 Punkt 3) ist **nicht** gelaufen.

### Was der Integrator verdrahtet oder geändert hat

| Datei | Änderung |
|---|---|
| `registry/ModEntities.java` | 29 Typen in `registerModEntity`-Zeilenfolge: Kühe (:3247–:3263) nach `GIRLFRIEND`; `CAMARASAURUS`, `HYDROLISC`, `VELOCITY_RAPTOR`, `DRAGONFLY`, `BARYONYX`, `BIRD`, `RUBY_BIRD` (:3415–:3499) nach `UNSTABLE_ANT`; `LIZARD` … `BEAVER` (:3559–:3805) nach `HOVERBOARD`; `PEACOCK`, `FLOUNDER`, `WHALE`, `COIN` (:3829–:3937) nach `TERMITE`; `CRYSTAL_APPLE_COW` (:4033) nach `BOYFRIEND`; `CRICKET`, `FROG` (:4109–:4129) nach `ROCK`. Werte wie geliefert; einzige Korrektur: Cricket `clientTrackingRange(4 / 2)` als `2` geschrieben |
| `registry/ModItems.java` | 28 Eier in Legacy-Id-Reihenfolge: 9106–9149 (16 Stück) vor `URANIUM_NUGGET`, 9166/9168/9171/9173 nach `GODZILLA_SCALE`, 9220 nach `PEACHTREE_SEED`, 9375/9376/9384/9385 nach `SKATE_BOW`, 9431 nach `BATTLE_AXE`, 9462/9464 nach `DUCTTAPE_ITEM`. `RubyBird` hat im Manifest kein Ei |
| `platform/CommonSetup.java` | 29 Attributsätze; 29 Spawn-Placements (`REPLACE`): Kühe und Hydrolisc `Animal::checkAnimalSpawnRules`, Flounder/Whale/Lizard `IN_WATER`, Frog `Frog.WATER_OR_GROUND`, alle übrigen `ON_GROUND` mit ihrem portierten `getCanSpawnHere` |
| `client/ClientSetup.java` | 29 Renderer (Schatten/Skalierung laut Portierer, `event.<Cockateil>registerEntityRenderer` für `RUBY_BIRD`), 24 Layer-Definitionen; die Kühe nutzen den Vanilla-`ModelLayers.COW` |
| `data/minecraft/tags/entity_type/can_breathe_under_water.json` | ergänzt: `gold_fish`, `flounder`, `whale`, `frog`, `beaver`, `hydrolisc` (je `canBreatheUnderwater() = true` im Original; in 1.21.1 final, W04-Präzedenz) |
| `entity/ai/EntityAIAvoidEntity.java` | **zusammengeführt** (R21, keine doppelten Hilfsklassen): `w06-cannonfodder` und `w06-herbivores` hatten beide den Vanilla-1.7.10-Goal `tw` portiert, verhaltensgleich (erstes lebendes sichtbares Ziel, zahme Tiere fliehen nie vor Spielern, Spieler inklusive Kreativ, `getPosAway(16, 7)`, Spaltentest mit `Mth.floor`). Die Herbivoren-Fassung ohne SRG-Feldnamen liegt jetzt in `entity.ai`; beide Paketkopien gelöscht, Imports in Chipmunk, Gazelle, Baryonyx, Beaver, Camarasaurus, Cassowary, Hydrolisc, Peacock und StinkBug ergänzt |
| `gametest/W06GameTests.java` | neu, siehe unten |

### GameTests W06

`<typ>SpawnsAndTicks` × 29: der Typ spawnt über seine Registry-Fabrik, hat die Original-Maximalgesundheit
(`manifest.json` → `max_health`, Kühe `EntityCow` 10) und lebt nach 100 Ticks noch. **Ein Test je Typ**, nicht eine
gemeinsame Arena wie in W05: mehrere W06-Mobs jagen einander (Frog frisst Cricket, die Cannon-Fodder-Familie kämpft
nach Hutfarbe), eine gemeinsame Arena würde an Originalverhalten statt an einem Portfehler scheitern.

Die R21-Tests von `w06-crosscut-r21` liefen im selben Lauf mit (Zählung: 99 Testwelle W05 + 2 Fixwelle W05 + 3 R21 +
29 W06 = 133; einzelne Testnamen protokolliert der `GameTestServer` bei Erfolg nicht). Damit ist die Fernschreib-Frage
für `ChunkProviderOreSpawn2.applyBiomeDecoration` für die erzeugten 5×5-Felder beantwortet: **kein**
`Detected setBlock in a far chunk`, das Populate-Fenster von Mining bleibt unverschoben.

Nicht getestet – Aufgabe der Testwelle, die Vorschläge der Portierer stehen in ihren Übergaben: Drops je Kuh und
Coin, Wal ohne Wasser, Frosch-Kuss, Ostrich-Reiten mit Fly-up-Payload, VelocityRaptor-Grasfressen und Spielertempo,
Zähmen und Züchten mit Kristallapfel, Beaver-Baumfällen, StinkBug-Übelkeit, Hydrolisc-Lebensentzug, Hut-Teams,
Dispenser je Ei.

### Was die nächsten Wellen wissen müssen

**Holder.** Entities (Klasse in Klammern, wo abweichend): `APPLE_COW` (`RedCow`), `GOLDEN_APPLE_COW` (`GoldCow`),
`ENCHANTED_GOLDEN_APPLE_COW` (`EnchantedCow`), `CRYSTAL_APPLE_COW` (`CrystalCow`), `GOLD_FISH`, `FLOUNDER`, `WHALE`,
`FROG` (`entity.aquatic.Frog`, nicht Vanilla), `DRAGONFLY`, `BIRD` (`Cockateil`), `RUBY_BIRD`, `T_SHIRT` (`Tshirt`),
`CLIFF_RACER`, `COIN`, `CRICKET`, `CAMARASAURUS`, `HYDROLISC`, `BARYONYX`, `STINK_BUG`, `CASSOWARY`, `BEAVER`,
`PEACOCK`, `LIZARD`, `CHIPMUNK`, `GAZELLE`, `VELOCITY_RAPTOR`, `OSTRICH`, `GHOST`, `GHOST_PUMPKIN_SKELLY`
(`GhostSkelly`). Eier: `EGG_RED_COW`, `EGG_GOLD_COW`, `EGG_ENCHANTED_COW`, `EGG_CRYSTAL_COW`, `EGG_GOLD_FISH`,
`EGG_FLOUNDER`, `EGG_WHALE`, `EGG_FROG`, `EGG_DRAGONFLY`, `EGG_COCKATEIL`, `EGG_TSHIRT`, `EGG_CLIFF_RACER`,
`EGG_COIN`, `EGG_CRICKET`, `EGG_CAMARASAURUS`, `EGG_HYDROLISC`, `EGG_BARYONYX`, `EGG_STINK_BUG`, `EGG_CASSOWARY`,
`EGG_BEAVER`, `EGG_PEACOCK`, `EGG_LIZARD`, `EGG_CHIPMUNK`, `EGG_GAZELLE`, `EGG_VELOCITY_RAPTOR`, `EGG_OSTRICH`,
`EGG_GHOST`, `EGG_GHOST_SKELLY`. `EGG_COIN` (Coin-Drop) und `EGG_PEACOCK` (Peacock legt es) werden per Name gelesen.

**Aufruf-Verträge.**

- `EntityCannonFodder` (Basis von Chipmunk, Lizard, Ostrich, VelocityRaptor): Konstruktor
  `(EntityType<? extends EntityCannonFodder>, Level)`; Original-`interact` = `protected boolean legacyInteract(Player)`
  mit `super.legacyInteract` zuerst; `updateAITick()`/`updateAITasks()` protected mit `super`-Aufruf; Drops über
  `dropFewItems(boolean, int)`/`getDropItem()`. `isVelocityRaptor()` vergleicht die Registry-Id
  `orespawn:velocity_raptor` – nicht umbenennen. `CannonFodderInteractEvents` stellt die 1.7.10-Klickreihenfolge
  (Namensschild, fremde Leine) für die ganze Familie her, `HerbivoreInteractEvents` dasselbe für die Herbivoren.
- Vanilla-1.7.10-Fluchtgoal: `entity.ai.EntityAIAvoidEntity(PathfinderMob, Class, float dist, double far, double near)`;
  OreSpawns eigener bleibt `entity.ai.MyEntityAIAvoidEntity`.
- Nachwuchs: die Kühe, Whale, Flounder und die Herbivoren rufen `ModEntities.X.get().create(level)`; Holder-Namen
  und generische Typen sind tragend.
- Marker: `Cockateil`/`RubyBird`, `Cricket`, `Dragonfly`, `Ghost`, `GhostSkelly` sind `Ignoreable`; `Ghost` und
  `GhostSkelly` zusätzlich `NoStepTrigger` (R20). `Coin`, `Tshirt`, `Hydrolisc` und `EntityCannonFodder`
  implementieren `LegacyArmor` (R5).

**Rückwärtskanten, die als `PORT: TODO` im Code stehen oder von den Portierern gemeldet wurden.**

| Welle | Stelle |
|---|---|
| W07 | `Frog.isSuitableTarget` – `instanceof WormSmall` (Frog.java:237) |
| W08 | `Lizard.isSuitableTarget` – `instanceof AttackSquid` (Lizard.java:332-334); CloudShark zielt auf `entity.critter.Cockateil` und `CliffRacer` |
| W10 | ThePrincess.java:627 / TheQueen.java:437 beschwören `ModEntities.BIRD` |
| W11 | Käfige der W06-Mobs (`cagedragonfly`, `cagecricket`, `cagecockateil`, `cagecliffracer`, `cagechipmunk`, `cagelizard`, `cagegazelle`, `cageostrich`, `cagevelocityraptor`, Kühe, Fische); die Dried-Egg-Blöcke für die W06-Mobs sind **nicht** auf die neuen Typen verdrahtet |
| W12 | natürliche Spawns (BirdEnable, CoinEnable, CricketEnable, DragonflyEnable, CowEnable, GoldFishEnable, FlounderEnable, WhaleEnable, FrogEnable, OstrichEnable, GhostEnable, GhostSkellyEnable, Halloween-Geister, Cassowary/Beaver/Peacock/StinkBug/Hydrolisc, Chipmunk/Lizard); Dimensionslisten (`BiomeGenUtopianPlains`, `ChunkProviderOreSpawn2.java:394/:397` VelocityRaptor, Dragonfly, Baryonyx, Camarasaurus); `RubyBirdDungeon`-Spawner `orespawn:ruby_bird`; Dried-Egg-Wassereimer-Rezepte. Die Placement-Prädikate stehen schon |

### Offene Punkte und Entscheidungen

1. **`getCanSpawnHere` bei der Chunk-Generierung** (Befund `w06-herbivores`): 1.7.10
   `SpawnerAnimals.performWorldGenSpawning` fragt nur `canCreatureTypeSpawnAtLocation`, nie `getCanSpawnHere`;
   1.21.1 `NaturalSpawner.spawnMobsForChunkGeneration` fragt das Placement-Prädikat. Die Forge-Patches von 1.7.10
   liegen nicht vor, also unbelegt. Wie in W04/W05 unverändert gelassen; braucht eine Zeile in `DECISIONS.md`, bevor
   W12 die Biomspawns einhängt.
2. **Dauer des VelocityRaptor-Spielertempos** (Befund `w06-riders-ghosts`, R18 „serverseitiger
   `AttributeModifier`“): wie lange der Modifikator bleibt, ist nicht festgelegt; siehe
   `RiderSupport.setVelocityRaptorSpeed`.
3. **Doppelte Helfermethoden** (R21 sinngemäß, nicht umgesetzt): `legacyPanic` liegt fünfmal
   (`companion/CompanionSupport`, `portal/EntityAnt`, `critter/CritterSupport`, `herbivore/HerbivoreSupport`,
   `cannonfodder/CannonFodderSupport`; die drei W06-Kopien textgleich), `consumeOne` dreimal (Companion, Herbivore,
   CannonFodder), `lootingLevel` dreimal (Herbivore, CannonFodder, `cow/RedCow`; bis auf den Variablennamen gleich),
   `spawnerNearby` viermal mit verschiedenen Signaturen (Companion, Ghost, Herbivore, Insect). Zusammenführen berührt
   Aufrufer aus W04/W05; eigener Aufräumschritt nach `entity.ai`, danach der volle GameTest-Lauf als Gegenprobe.
4. **R21, Crystal und Chaos** laufen weiter über Vanilla-Biom-Features (Abschnitt „Offen“ oben).
5. **W05-Hinweis weiter offen:** `EntityAIAttackOnCollide` und `EntityAINearestAttackableTarget` liegen noch in
   `entity.portal`, nicht in `entity.ai`.
6. **Nicht W06, beim Abgleich aufgefallen:** `egggirlfriend` (9105) und `eggboyfriend` (9399) stehen im Manifest,
   sind aber nicht registriert – Lücke aus W04.
7. **Unbelegte Client-Details:** ob 1.21.1 den Schatten von Baby-Mobs halbiert (1.7.10 nicht) und ob Mobs ohne
   Velocity-Updates ruckeln – ohne Client nicht beurteilbar.

## Fixwelle (Review-Befunde, 2026-09-11)

Drei undokumentierte Abweichungen aus dem Review, jede an ihren Geschwistern in derselben Welle mitbehoben.

| Befund | Ursache | Fix |
|---|---|---|
| RedCow (und Gold-/Enchanted-/CrystalCow) wandert 3× so oft, schaut 1,5× so oft, Tempt-Pause 100 statt 300 Ticks | Vanilla-1.21.1-Goals würfeln jeden 2. Tick, 1.7.10 jeden 3.; `TemptGoal.calmDown = reducedTickDelay(100)` | Vier Ports der 1.7.10-Vanilla-Goals in `entity.ai`, alle über `LegacyAiTick`: `EntityAITempt` (`vk`), `EntityAIWander` (`vc`), `EntityAIWatchClosest` (`un`), `EntityAILookIdle` (`vb`), je mit `javap -c` gegen `client-1.7.10.jar` geprüft |
| Beaver: Laub eines gefällten Baums zerfällt nie | `LEGACY_FLAG_2` enthält `UPDATE_KNOWN_SHAPE`, 1.21.1 startet Laubzerfall nur über `LeavesBlock.updateShape`; 1.7.10 rief `breakBlock` unabhängig von den Flags | `HerbivoreSupport.legacyLogBreakBlock` (Port von `BlockLog.breakBlock`, `alx.a`: Radius 4, Chunkprüfung 5) plant für jedes `LeavesBlock` einen Tick; Beaver ruft es nach jedem entfernten `Blocks.log` (Eiche/Fichte/Birke/Tropen, Stamm und Holz), nicht für Zaun, Tor, Schild und die zwei OreSpawn-Stämme (`extends Block`) |
| Whale-Kalb frisst den Kristallapfel und wächst | geerbtes `Animal.mobInteract` füttert Babys (`ageUp`) | `mobInteract` → `HerbivoreSupport.legacyAnimalInteract` |

Geschwister, die denselben Fehler hatten:

- Vanilla-Goals ersetzt in Whale, Flounder, Chipmunk, Gazelle, Lizard, Camarasaurus, Cassowary, Peacock, Beaver,
  StinkBug, VelocityRaptor, Hydrolisc, Ostrich, Baryonyx. Coins privates `LegacyLookIdle` ist jetzt das geteilte
  `EntityAILookIdle` (R21, keine Doppelkopien). Die Kommentare „TemptGoal prüft beide Hände" sind entfernt:
  `EntityAITempt` nimmt wie 1.7.10 den nächsten Spieler und prüft dessen Haupthand.
- Camarasaurus frisst Vanilla-Laub mit Flag 2: `HerbivoreSupport.legacyLeavesBreakBlock` (Port von
  `BlockLeaves.breakBlock`, `alt.a`, Radius 1).
- Flounder und GoldFish erbten ebenfalls `Animal.mobInteract`: jetzt `legacyAnimalInteract`.

Verhaltensänderungen, die man sehen kann: `EntityAITempt` hebt wie `vk` während des Lockens die Wassermeidung auf
(Malus gesichert, 0, zurückgesetzt – dieselbe Übersetzung wie `MyEntityAIFollowOwner`); RedCow und Camarasaurus
(Malus −1) gehen einem lockenden Spieler also durchs Wasser nach. `EntityAIWander` stoppt die Navigation am Ende nicht
(`vc` hat kein `resetTask`). `Beaver.fellAt` ist der herausgezogene Griefing-Zweig von `updateAITick` (:216-218), nur
damit ein GameTest fällen kann.

GameTests (in `W06GameTests`, Abschnitt „fix wave"): `temptCalmsDownForAHundredEvaluationsOnTheLegacyCadence` (bereit
genau 300 Ticks nach dem Reset, Nebenhand lockt nicht), `whaleCalfIsNotFedACrystalApple`,
`youngFlounderIsNotFedACrystalApple`, `leavesOfATreeTheBeaverFellsStartToDecay` (fällt 10 Ticks nach dem Aufbau, damit
die eigenen Platzierungs-Ticks des Laubs den Test nicht ohne Port grün machen).

Offen für den Integrator (nicht in W06-Dateien):

- `companion/Girlfriend.java:172-182` und `companion/Boyfriend.java:140-148` (W04) nutzen noch `TemptGoal`,
  `LookAtPlayerGoal` und `RandomLookAroundGoal` – derselbe Kadenzbefund. Die Ports liegen jetzt in `entity.ai`.
- `RedCow` behält den dokumentierten PORT zu `Cow.mobInteract` (Kalb nicht melkbar, Weizen lässt Kälber wachsen); das
  ist dieselbe Befundklasse wie beim Wal, war aber schon als Abweichung vermerkt und nicht Teil des Reviews.
